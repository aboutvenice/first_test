package {	import flash.display.MovieClip;	import flash.display.Sprite;	import flash.display.Graphics;	import flash.events.Event;	public class Main extends MovieClip	{		var layer1:Sprite=new Sprite();		var moveX:Number;		var widthL:int=0		var heightL:int=0		var widthR:int=stage.stageWidth		var heightR:int=stage.stageHeight				public function Main()		{			addChild(layer1);			createBall();			//getBall();			stage.addEventListener(Event.ENTER_FRAME,onRunHandler);		}		public function createBall()		{			for (var i:int=0; i<10; i++)			{				var color:int = Math.random() * 0xFF0000;				var ball:Sprite=new Sprite();				ball.graphics.beginFill(color);				ball.graphics.drawCircle(10,10,50);				ball.x = Math.random() * stage.stageWidth;				ball.y = Math.random() * stage.stageHeight;				layer1.addChild(ball);				moveX=Math.floor((Math.random()*3)-1);				if(moveX==0) moveX=1				ball.name=String(moveX)			}		}		public function getBall()		{			for (var i:int=0; i<10; i++)			{				moveX=Math.floor((Math.random()*3)-1);				//nowBall.addEventListener(Event.ENTER_FRAME,onRunHandler);			}		}		public function onRunHandler(e:Event)		{			for (var i:int=0; i<10; i++)			{				var nowBall:Object = layer1.getChildAt(i);				//				nowBall.x+=10*int(nowBall.name)				nowBall.y+=10*int(nowBall.name)				if((nowBall.x<widthL)||(nowBall.x>widthR)) nowBall.name*=-1								if((nowBall.y<heightL)||(nowBall.y>heightR)) nowBall.name*=-1							}					}	}}